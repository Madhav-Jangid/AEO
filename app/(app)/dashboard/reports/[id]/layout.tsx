import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Report Details",
};

export default function DashboardReportDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
